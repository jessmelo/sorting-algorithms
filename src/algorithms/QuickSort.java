package algorithms;

public class QuickSort {
    public static void sort(int[] arr) {
        // get initial pivot element
        int pivot = arr[0];
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] < pivot) {
                int temp = arr[i];
                
            }
        }
    }
}
